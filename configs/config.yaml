mqtt:
  broker: "ssl://broker.emqx.io:8883"
  client_id: "agent-001"
  ca_certificate: "certs/broker.emqx.io-ca.crt"

services:
  heartbeat:
    topic: "iot/agent/heartbeat"
    enabled: true
    interval: 10
