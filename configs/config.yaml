mqtt:
  broker: "ssl://broker.emqx.io:8883"
  client_id: "iot_agent-"
  ca_certificate: "certs/broker.emqx.io-ca.crt"

services:
  heartbeat:
    topic: "iot-heartbeat"
    enabled: true
    interval: 10
